
var WorkQueue = function(){

}

WorkQueue.prototype.createJob = function(first_argument) {
  // body...
};

WorkQueue.prototype.pushJob = function(first_argument) {
  // body...
};

WorkQueue.prototype.pause = function(first_argument) {
  // body...
};

WorkQueue.prototype.resume = function(first_argument) {
  // body...
};


var WorkQueueManager = function(params) {
  // body...
}

WorkQueueManager.prototype.createQueue = function(events, eventBus) {
  
};


module.exports = WorkQueueManager;
